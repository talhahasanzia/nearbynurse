# Frontend Environment Variables Example

# Backend API URL
VITE_API_URL=http://localhost:3000

# Keycloak Configuration
# URL to your Keycloak server
VITE_KEYCLOAK_URL=http://localhost:8080

# Keycloak realm name
VITE_KEYCLOAK_REALM=master

# Keycloak client ID (must match client created in Keycloak admin console)
VITE_KEYCLOAK_CLIENT_ID=nearbynurse-frontend

# App Base URL (used for OAuth2 redirects)
# This must match the Valid Redirect URIs in Keycloak client settings
VITE_APP_BASE_URL=http://localhost:5173

